const my = require('./my-module')
// you dont need to write the js, its assumed because you are running node and node is JavaScript.

console.log(my);

// closures still work
  // my.getNumber();
  // console.log(my.arr);

console.log(Tomogotchi);
